import {StyleSheet} from 'react-native';

/**
 * Define list of style sheet to design indicator component.
 */
const Styles = StyleSheet.create({
  default: {
    alignSelf: 'center',
  },
});

export default Styles;
